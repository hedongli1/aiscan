# 🛠️ aiscan 自定义扫描与技术实现指南

> 本文系统解答：**aiscan 如何实现自定义扫描**、**底层技术路线与实现原理**、以及**开发者日常操作指南**。

---

## 🧭 一、自定义扫描的核心技术路线与架构

`aiscan` 是一套**零依赖原生 Node.js** 编写的代码安全审计与 GitHub 自动化巡检引擎。它通过**三层架构**实现了不同粒度的灵活自定义：

```
                    ┌─────────────────────────┐
                    │     用户调用层 (CLI)     │
                    │  自定义路径 / 过滤级 / 模式 │
                    └────────────┬────────────┘
                                 │
     ┌───────────────────────────┼───────────────────────────┐
     ▼                           ▼                           ▼
【维度 1: 扫描对象定制】     【维度 2: 规则与阈值定制】    【维度 3: 误报与白名单定制】
- 任意本地文件夹/单文件     - 严重级门禁(--severity)      - .aiscanignore (路径/文件)
- 指定开源仓库(--patrol-repo)- 失败门禁(--fail-on)         - 行级注释(// aiscan-disable)
- 组织批量(--patrol-targets)- 自定义规则库扩展            - 敏感词/测试用例放行
- 每日限额(maxPerDay)
     │                           │                           │
     └───────────────────────────┼───────────────────────────┘
                                 ▼
                    ┌─────────────────────────┐
                    │      底层核心引擎       │
                    │ 正则特征匹配 + Shannon  │
                    │ 熵计算 + AST上下文启发  │
                    └────────────┬────────────┘
                                 ▼
                    ┌─────────────────────────┐
                    │      产物与看板呈现     │
                    │ 控制台 / JSON / HTML /  │
                    │ GitHub Actions / Pages  │
                    └─────────────────────────┘
```

---

## 🔍 二、实现自定义的核心原理

### 1. 扫描目标自定义（Target Customization）
- **本地扫描**：`lib/scanner.js` 会递归扫描指定目标。你可以传入任意相对路径或绝对路径（如 `aiscan ./src ./server`），引擎会自动遍历并根据后缀筛选（支持 `.js`, `.ts`, `.vue`, `.json`, `.env` 等）。
- **云端开源巡检自定义**：
  - 配置文件 `patrol-targets.json` 提供了最高优先级的目标控制开关；
  - 若 `autoDiscover: true`，底层通过 GitHub Search API 根据星数、更新时间和日期哈希种子自动轮换；
  - 若 `autoDiscover: false`，则直接读取用户在 `repos: ["owner/repo1", "owner/repo2"]` 填入的自定义清单；
  - 设有严格的 **Token 自扫防御逻辑**，无论怎么自定义，绝不误扫用户本人名下资产。

### 2. 规则与阈值自定义（Rule & Severity Customization）
- **严重性过滤**：通过 `--severity=high` 或 `--fail-on=critical`，在发现列表输出时通过权重矩阵进行即时过滤与退出码阻断（Exit Code 0/1/2）。
- **规则扩展机制**：所有规则集中定义于 `lib/rules/`。每条规则为一个纯函数对象，具备：
  - `id`: 唯一规则标识符（如 `SECRET-AWS`, `INJ-SQL-CONCAT`）
  - `severity`: 默认严重级别（`critical` / `high` / `medium` / `low`）
  - `pattern`: 匹配正则表达式或上下文启发函数
  - `message`: 风险提示与修复建议

### 3. 误报豁免与白名单（Ignore & Allowlist）
- **工程级忽略**：支持 `.aiscanignore`，采用 glob 语法过滤掉测试用例、静态字典、编译产物等。
- **行级精准抑制**：在代码行后添加 `// aiscan-disable` 或 `// aiscan-disable-line RULE-ID` 可按需单点豁免。

---

## 🚀 三、如何操作自定义扫描（实战手册）

### 场景 1：本地自定义扫描特定目录或文件
如果你只想扫描项目里的后端逻辑或者某个特定脚本：
```bash
# 扫描单个目录
aiscan ./server/src

# 同时扫描多个指定目录并输出可视化 HTML 报告
aiscan ./server ./web --report html

# 只查看 high 及以上级别的漏洞
aiscan ./src --severity=high

# CI 场景：只要有 critical 漏洞就返回失败阻止合并
aiscan ./ --fail-on=critical
```

### 场景 2：自定义远程开源巡检目标（定点排查）
如果你想让 GitHub Action 定时自动巡检你指定的几个开源项目：

1. 打开仓库里的 `patrol-targets.json`：
```json
{
  "autoDiscover": false,
  "maxPerDay": 10,
  "repos": [
    "expressjs/express",
    "axios/axios",
    "vuejs/core"
  ]
}
```
2. 提交推送到 GitHub，工作流下一次触发时就会**定点只扫描这几个项目**，并在 Pages 看板中生成它们的安全评分与风险报告。

### 场景 3：新增一条你自己的安全检测规则
如果你的团队有自定义业务规范（例如：禁止在前端代码中调用 `localStorage.setItem('token', ...)`）：

1. 在 `lib/rules/` 下创建或编辑规则：
```javascript
export const ruleLocalStorageToken = {
  id: 'SEC-LOCALSTORAGE-TOKEN',
  severity: 'high',
  category: 'Storage',
  message: '禁止在 localStorage 中明文存储 Token，存在 XSS 窃取风险',
  check: (line) => /localStorage\.setItem\s*\(\s*['"`]token['"`]/i.test(line),
};
```
2. 注册到 `lib/rules/index.js`，运行 `aiscan --rules` 即可看到新规则生效！

---

## 📊 四、输出与看板对接
- `--json`：结构化 JSON 输出，用于对接后端看板、告警通知或流水线。
- `--sarif`：标准 SARIF 2.1.0 格式，可直接上传到 GitHub **Security Code Scanning Alerts** 官方看板。
- `docs/index.html`：轻量极简的前端看板，直接读取每日落盘的 JSON 文件，在 GitHub Pages 上提供免运维可视化交互。
