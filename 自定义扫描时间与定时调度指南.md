# ⏰ aiscan 自定义扫描时间与定时调度完整指南

> 本文系统解答：**GitHub Actions 中如何自定义代码安全扫描的时间与周期**、**底层调度原理**、以及**如何修改生效并推送到 GitHub**。

---

## 🧭 一、核心机制：GitHub Actions 定时调度 (Cron 触发器)

GitHub 平台的定时扫描基于 Linux 标准 **POSIX cron 语法**。系统由 GitHub 内部的时间调度集群按 UTC 时间触发执行。

### 1. 触发定义位置
aiscan 的云端自动化巡检工作流位于：
- `.github/workflows/patrol.yml`（每日代码审计巡检与看板生成）
- `.github/workflows/ci.yml`（Push 与 PR 时的即时门禁）

在 `patrol.yml` 中，定时调度通过 `on.schedule` 块定义：

```yaml
on:
  schedule:
    # 语法格式：分 时 日 月 星期 (UTC 时间)
    - cron: '0 3 * * *'
  workflow_dispatch: # 同时支持手动按需随时触发
```

---

## 🕒 二、常用自定义时间配置示例对照表

> ⚠️ **注意时区换算**：GitHub Actions 的服务器一律使用 **UTC 世界标准时间**。
> 北京时间（CST）为 **UTC + 8 小时**。

| 目标频次 | Cron 表达式 | UTC 时间 | 北京时间 (UTC+8) | 适用场景 |
| :--- | :--- | :--- | :--- | :--- |
| **每天 1 次（清晨）** | `0 3 * * *` | 每日 03:00 | **每日 11:00** | **默认推荐**，每日自动巡检一次最新提交 |
| **每天 1 次（深夜）** | `0 16 * * *` | 每日 16:00 | **次日 00:00 (午夜)** | 适合夜间非业务高峰期跑批 |
| **每天 2 次（每 12 小时）** | `0 2,14 * * *` | 02:00 与 14:00 | **10:00 与 22:00** | 适合高频开源项目或敏捷迭代团队 |
| **每 6 小时扫描一次** | `0 */6 * * *` | 00, 06, 12, 18 点 | **08, 14, 20, 02 点** | 适合对安全漏洞敏感的生产级仓库 |
| **工作日每天一次** | `0 1 * * 1-5` | 周一到周五 01:00 | **周一到周五 09:00** | 专为上班工时设计的定时流水线 |
| **每周一次（周一）** | `0 3 * * 1` | 每周一 03:00 | **每周一 11:00** | 适合归档型或非频繁变动的代码库 |

---

## 🛠️ 三、如何修改并生效（操作步骤）

### 第一步：修改 Workflow 文件
打开并编辑 `aiscan/.github/workflows/patrol.yml`，将 `cron` 行替换为你希望的时间：

```yaml
on:
  schedule:
    # 例如：改成每天上午 10:00 和晚上 22:00 各执行一次（UTC 02:00, 14:00）
    - cron: "0 2,14 * * *"
```

### 第二步：提交并推送到 GitHub
```bash
git add .github/workflows/patrol.yml
git commit -m "chore(ci): update patrol schedule to run twice daily"
git push origin main
```
推送至 `main` 分支后，GitHub 会立即解析并注册新的定时器，**无需重启任何服务，全自动生效**。

### 第三步：如何随时“手动临时触发”（无需等待定时器）
如果在修改完代码后，想**立即看到扫描结果**，可以通过 `workflow_dispatch` 手动触发：
1. 打开你的 GitHub 仓库主页：`https://github.com/hedongli1/aiscan`
2. 点击顶部导航的 **Actions** 标签页
3. 在左侧选择 **`aiscan-patrol`** 工作流
4. 点击右侧的 **Run workflow** 下拉按钮，即可立即手动启动！
5. 在输入框中还支持临时自定义要扫描的仓库和最低漏洞等级。

---

## 🛡️ 四、本地定时扫描（离线环境自运行）

如果你想在自己的服务器或本地电脑上定时跑 `aiscan`，可以结合 Linux 的 `crontab`：

```bash
# 1. 打开本地 crontab 编辑器
crontab -e

# 2. 添加定时任务（例如每天凌晨 2 点执行本地扫描并输出 HTML 报告）
0 2 * * * cd /path/to/your/project && /usr/bin/node /path/to/aiscan/bin/aiscan.js ./ --report html >> /tmp/aiscan.log 2>&1
```
